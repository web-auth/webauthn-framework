{% extends '@WebProfiler/Profiler/layout.html.twig' %}

{% import _self as helper %}

{% block toolbar %}
    {% set icon %}
        {% include '@Webauthn/data_collector/_icon.html.twig' %}
    {% endset %}

    {% set text %}
        <div class="sf-toolbar-info-piece">
            <b>Public Key Credential Attestation Options</b>
            <span class="sf-toolbar-status">{{ collector.getData().publicKeyCredentialCreationOptions|length }}</span>
        </div>
        <div class="sf-toolbar-info-piece">
            <b>Authenticator Attestation (success/failure)</b>
            <span
                class="sf-toolbar-status sf-toolbar-status-green">{{ collector.getData().authenticatorAttestationResponseValidationSucceeded|length }}</span>&nbsp;/<span
                class="sf-toolbar-status sf-toolbar-status-red">{{ collector.getData().authenticatorAttestationResponseValidationFailed|length }}</span>
        </div>
        <div class="sf-toolbar-info-piece">
            <b>Public Key Credential Creation Options</b>
            <span class="sf-toolbar-status">{{ collector.getData().publicKeyCredentialCreationOptions|length }}</span>
        </div>
        <div class="sf-toolbar-info-piece">
            <b>Authenticator Assertion (success/failure)</b>
            <span
                class="sf-toolbar-status sf-toolbar-status-green">{{ collector.getData().authenticatorAssertionResponseValidationSucceeded|length }}</span>&nbsp;/<span
                class="sf-toolbar-status sf-toolbar-status-red">{{ collector.getData().authenticatorAssertionResponseValidationFailed|length }}</span>
        </div>
    {% endset %}

    {{ include('@WebProfiler/Profiler/toolbar_item.html.twig', { 'link': true }) }}
{% endblock %}

{% block menu %}
    <span class="label">
        {% include '@Webauthn/data_collector/_icon.html.twig' %}
        <strong>Webauthn</strong>
    </span>
{% endblock %}

{% block panel %}
    <div class="sf-tabs">
        {% include '@Webauthn/data_collector/tab/attestation.html.twig' %}
        {% include '@Webauthn/data_collector/tab/request.html.twig' %}
    </div>
{% endblock %}
